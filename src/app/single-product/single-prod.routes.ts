import { Route } from "@angular/router";
import { SingleProductComponent } from "./single-product.component";

export const SingleProdRoute: Route[] = [
    {
        path: '',
        component: SingleProductComponent
    }
]